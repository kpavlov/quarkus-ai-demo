services:

  weather-service:
    image: acme/weather-mcp-server:latest

  ai-service:
    image: acme/ai-server:latest
    environment:
      QUARKUS_PROFILE: test
    depends_on:
      - weather-service

